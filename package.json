{
  "name": "eigen-fullstack",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev:backend": "pnpm --filter eigen-backend start:dev",
    "dev:frontend": "pnpm --filter eigen-frontend dev",
    "build": "pnpm -r build",
    "test": "pnpm -r test"
  }
}